Run `yarn workspace my-app lint:hbs` to see a lint error in the terminal.

This error will not appear in VSCode in the file `app/templates/application.hbs`
`code packages/my-app/app/templates/application.hbs`
